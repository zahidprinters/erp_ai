# ---------------------------------------------------------------------------
# Multi-step workflows — complex operations that create multiple documents.
#
# Each workflow takes a JSON data dict and returns a result dict.
# Workflows use the MCP tool layer for all DB access.
# ---------------------------------------------------------------------------
